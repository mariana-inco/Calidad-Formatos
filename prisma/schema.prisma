generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model RegistroGestionCambio {
  id                     String   @id @default(cuid())
  numero                 Int      @unique @default(autoincrement())
  code                   String   @unique
  company                String
  process                String
  leaderUserId           String?
  leaderName             String
  creatorUserId          String
  creatorName            String
  currentState           String   @default("BORRADOR")
  currentResponsibleRole String
  currentResponsibleId   String?
  currentResponsibleName String?
  selectedApproverId     String?
  selectedApproverName   String?
  selectedApproverRole   String?
  changeTypes            String
  analysis               String
  implementationPlan     String
  qualityValidation      String?
  correctionNotes        String?
  approvedAt             DateTime?
  followupStartedAt      DateTime?
  closeDueAt             DateTime?
  closedAt               DateTime?
  createdAt              DateTime @default(now())
  updatedAt              DateTime @updatedAt

  movimientos MovimientoGestionCambio[]

  @@index([company])
  @@index([process])
  @@index([creatorUserId])
  @@index([leaderUserId])
  @@index([currentState])
  @@index([currentResponsibleRole, currentResponsibleId])
  @@map("registros_gestion_cambios")
}

model MovimientoGestionCambio {
  id                   String   @id @default(cuid())
  changeRequestId      String
  type                 String
  action               String
  fromState            String?
  toState              String
  userId               String?
  userName             String
  role                 String?
  position             String?
  observation          String?
  selectedApproverId   String?
  selectedApproverName String?
  selectedApproverRole String?
  approved             Boolean?
  signature            String?
  effective            Boolean?
  followupActions      String?
  createdAt            DateTime @default(now())

  registroGestionCambio RegistroGestionCambio @relation(fields: [changeRequestId], references: [id], onDelete: Cascade)

  @@index([changeRequestId])
  @@index([userId])
  @@index([type])
  @@index([createdAt])
  @@map("movimientos_gestion_cambios")
}
